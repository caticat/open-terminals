# Changelog

## v0.1.5

- Added support for placing `open-terminals.yml` under `.vscode/open-terminals.yml`
- Kept the workspace-root `open-terminals.yml` path working for backward compatibility
- `.vscode/open-terminals.yml` is preferred when both files exist
- Updated README and release notes to document the new config location

## v0.1.4

- Added `Open Terminals - Open One (Select)`
- Extracted shared terminal creation logic into `openTerminal()`
